import { HighlightTextSnippetPipe } from './highlight-text-snippet.pipe';

describe('HighlightTextSnippetPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightTextSnippetPipe();
    expect(pipe).toBeTruthy();
  });
});
